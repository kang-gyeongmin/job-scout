# job-scout 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 원티드·사람인·로켓펀치에서 데이터 엔지니어(신입·주니어, 수도권) 공고를 매일 수집하고, Claude 에이전트가 프로필 대비 적합도를 채점·요약해 네이버 메일로 발송하는 봇.

**Architecture:** 수집기는 결정적 Python 함수로 구현하고 Claude Agent SDK의 커스텀 툴(`search_jobs`)로 등록한다. 에이전트가 키워드를 바꿔가며 툴을 호출해 신규 공고를 모으고, profile.md와 비교해 점수·이유·요약을 JSON으로 출력한다. 이메일 발송·중복 관리는 코드가 결정적으로 수행한다.

**Tech Stack:** Python 3.13, uv, claude-agent-sdk, httpx, BeautifulSoup4(로켓펀치만), PyYAML, python-dotenv, pytest + pytest-asyncio, smtplib(표준 라이브러리), Windows 작업 스케줄러(schtasks)

## Global Constraints

- 작업 디렉터리: `C:\Users\Dell3571\Desktop\job-scout` (git 레포 초기화 완료, main 브랜치)
- Python 3.13 + uv 사용. 모든 실행은 `uv run ...`
- 비밀정보(ANTHROPIC_API_KEY, SARAMIN_API_KEY, SMTP_USER, SMTP_PASSWORD)는 `.env`에만 둔다. 절대 커밋 금지
- Claude 인증: `.env`에 `ANTHROPIC_API_KEY`가 있으면 그것을 쓰고, 없으면 PC의 Claude Code 구독 인증을 그대로 사용 (SDK 기본 동작 — 코드에서 키 존재를 강제하지 않는다)
- 수집 요청은 사이트당 실행 1회 기준 소량(키워드당 목록 1회 + 상세 최대 20회), 요청 간 0.5초 지연
- 공통 스키마 `JobPosting`의 필드명은 Task 2에 정의된 것을 모든 태스크가 그대로 사용
- 테스트는 네트워크 없이 fixture로 돌아가야 한다 (실제 API 확인은 별도 probe 스크립트)
- 이메일 수신/발신: `rkdrudals991@naver.com` (네이버 SMTP, SSL 465)
- 커밋 메시지 끝에 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` 추가

---

### Task 1: 프로젝트 스캐폴딩 + GitHub 레포

**Files:**
- Create: `pyproject.toml`, `.gitignore`, `.env.example`, `config.yaml`, `profile.md`, `src/__init__.py`, `src/collectors/__init__.py`, `tests/__init__.py`, `README.md`

**Interfaces:**
- Produces: 이후 모든 태스크가 사용하는 프로젝트 뼈대, `config.yaml`/`profile.md`의 형식

- [ ] **Step 1: uv 프로젝트 초기화 + 의존성 추가**

```powershell
Set-Location C:\Users\Dell3571\Desktop\job-scout
uv init --no-readme --python 3.13
Remove-Item main.py -ErrorAction SilentlyContinue  # uv init이 만든 샘플 제거
uv add claude-agent-sdk httpx pyyaml python-dotenv beautifulsoup4
uv add --dev pytest pytest-asyncio
```

- [ ] **Step 2: pyproject.toml에 pytest 설정 추가**

`pyproject.toml` 끝에 추가:

```toml
[tool.pytest.ini_options]
pythonpath = ["."]
asyncio_mode = "auto"
```

- [ ] **Step 3: .gitignore 작성**

```gitignore
.env
.venv/
__pycache__/
*.pyc
data/
logs/
reports/
```

- [ ] **Step 4: .env.example 작성**

```
# 없으면 Claude Code 구독 인증 사용 (선택)
ANTHROPIC_API_KEY=
# 사람인 오픈 API 키 (Task 8 전까지 비워둠)
SARAMIN_API_KEY=
# 네이버 메일 계정 (아이디@naver.com 전체)
SMTP_USER=rkdrudals991@naver.com
# 네이버 비밀번호 또는 2단계 인증 시 애플리케이션 비밀번호
SMTP_PASSWORD=
```

- [ ] **Step 5: config.yaml 작성**

```yaml
job_keywords:
  - "데이터 엔지니어"
  - "Data Engineer"
experience: "신입~주니어(3년 이하)"
locations: ["서울", "경기", "인천"]
sites:
  wanted: true
  saramin: false      # API 키 발급 후 true (Task 8)
  rocketpunch: false  # Task 9 완료 후 true
email:
  to: rkdrudals991@naver.com
  from: rkdrudals991@naver.com
  smtp_host: smtp.naver.com
  smtp_port: 465
min_score: 6   # 이 점수 이상만 이메일에 포함
top_n: 10      # 이메일에 담을 최대 공고 수
```

- [ ] **Step 6: profile.md 초안 작성**

```markdown
# 지원자 프로필

- 목표 직무: 데이터 엔지니어 (신입~주니어, 3년 이하)
- 희망 지역: 서울·경기·인천 (수도권)

## 기술 스택
- Python, SQL — 데이터 처리·파이프라인 작성
- Spark, Airflow, Kafka — 분산 처리, 워크플로 오케스트레이션, 스트리밍
- AWS(EC2, S3), Docker — 클라우드 인프라·컨테이너 환경 경험
- LLM/AI 활용 — LLM API, 에이전트 개발 경험 (본 프로젝트 포함)

## 선호
- 데이터 파이프라인 구축·운영 중심 포지션 선호
- 대규모 트래픽/데이터 환경 관심
```

(사용자가 이후 직접 수정한다 — 내용을 지어내서 늘리지 말 것)

- [ ] **Step 7: 빈 패키지 파일 + README 생성**

`src/__init__.py`, `src/collectors/__init__.py`, `tests/__init__.py`는 빈 파일. `README.md`:

```markdown
# job-scout

원티드·사람인·로켓펀치에서 조건에 맞는 채용 공고를 매일 수집해,
Claude 에이전트가 프로필 대비 적합도를 채점·요약하고 이메일로 보내주는 봇.

## 실행

​```powershell
uv run python -m src.main --dry-run   # 이메일 없이 콘솔 출력
uv run python -m src.main             # 실제 이메일 발송
​```

설정은 `config.yaml`, 판단 기준 프로필은 `profile.md`, 비밀값은 `.env`(예시: `.env.example`).
```

- [ ] **Step 8: 동작 확인**

Run: `uv run python -c "import claude_agent_sdk, httpx, yaml, bs4; print('ok')"`
Expected: `ok`

- [ ] **Step 9: 커밋 + GitHub 레포 생성·푸시**

```powershell
git add -A
git commit -m "chore: 프로젝트 스캐폴딩 (uv, 의존성, config, profile)"
gh repo create job-scout --public --source . --push
```

Expected: `https://github.com/kang-gyeongmin/job-scout` 생성 및 main 푸시 완료

---

### Task 2: JobPosting 모델 + SeenStore

**Files:**
- Create: `src/models.py`, `src/store.py`
- Test: `tests/test_store.py`

**Interfaces:**
- Produces:
  - `JobPosting` dataclass — 필드: `id: str`(예: `"wanted:12345"`), `site: str`, `title: str`, `company: str`, `location: str`, `experience: str`, `url: str`, `description: str`, `posted_at: str` (빈 값 허용: `description`, `posted_at`은 `""`)
  - `SeenStore(path: Path)` — `.filter_new(postings: list[JobPosting]) -> list[JobPosting]`, `.mark(ids: list[str]) -> None` (저장은 JSON 파일, 디렉터리 없으면 생성)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_store.py`

```python
from pathlib import Path
from src.models import JobPosting
from src.store import SeenStore


def make_posting(pid: str) -> JobPosting:
    return JobPosting(
        id=pid, site="wanted", title="데이터 엔지니어", company="회사",
        location="서울", experience="신입", url=f"https://x/{pid}",
        description="", posted_at="",
    )


def test_filter_new_excludes_marked_ids(tmp_path: Path):
    store = SeenStore(tmp_path / "data" / "seen.json")
    a, b = make_posting("wanted:1"), make_posting("wanted:2")
    store.mark(["wanted:1"])
    assert store.filter_new([a, b]) == [b]


def test_mark_persists_across_instances(tmp_path: Path):
    path = tmp_path / "seen.json"
    SeenStore(path).mark(["wanted:1"])
    assert SeenStore(path).filter_new([make_posting("wanted:1")]) == []
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_store.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'src.models'`

- [ ] **Step 3: 구현** — `src/models.py`

```python
from dataclasses import dataclass


@dataclass
class JobPosting:
    id: str          # "사이트:공고ID", 예: "wanted:12345"
    site: str        # wanted | saramin | rocketpunch
    title: str
    company: str
    location: str
    experience: str  # 사이트 원문 그대로
    url: str
    description: str  # 상세 본문, 없으면 ""
    posted_at: str    # ISO 날짜 문자열, 없으면 ""
```

`src/store.py`:

```python
import json
from pathlib import Path

from src.models import JobPosting


class SeenStore:
    def __init__(self, path: Path):
        self.path = Path(path)
        self._ids: set[str] = set()
        if self.path.exists():
            self._ids = set(json.loads(self.path.read_text(encoding="utf-8")))

    def filter_new(self, postings: list[JobPosting]) -> list[JobPosting]:
        return [p for p in postings if p.id not in self._ids]

    def mark(self, ids: list[str]) -> None:
        self._ids.update(ids)
        self.path.parent.mkdir(parents=True, exist_ok=True)
        self.path.write_text(
            json.dumps(sorted(self._ids), ensure_ascii=False, indent=1),
            encoding="utf-8",
        )
```

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_store.py -v`
Expected: 2 passed

- [ ] **Step 5: 커밋**

```powershell
git add src/models.py src/store.py tests/test_store.py
git commit -m "feat: JobPosting 모델과 SeenStore(중복 알림 방지) 추가"
```

---

### Task 3: 원티드 수집기

**Files:**
- Create: `src/collectors/wanted.py`, `scripts/probe_wanted.py`
- Test: `tests/test_wanted.py`, `tests/fixtures/wanted_list.json`

**Interfaces:**
- Consumes: `JobPosting` (Task 2)
- Produces: `search(keyword: str, limit: int = 20) -> list[JobPosting]` — 신입~3년 필터 적용된 원티드 공고. `parse_list(data: dict) -> list[JobPosting]` (fixture 테스트 대상)

원티드는 공식 API가 없고, 웹사이트가 내부적으로 쓰는 JSON API(`https://www.wanted.co.kr/api/v4/jobs`)를 호출한다. **응답 구조가 예고 없이 바뀔 수 있으므로 probe → fixture 저장 → fixture 기반 파서 TDD 순서를 반드시 지킨다.**

- [ ] **Step 1: probe 스크립트 작성 및 실행** — `scripts/probe_wanted.py`

```python
"""원티드 내부 API 응답을 확인하고 테스트 fixture로 저장한다."""
import json
from pathlib import Path

import httpx

HEADERS = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"}
PARAMS = {
    "country": "kr",
    "job_sort": "job.latest_order",
    "locations": "seoul.all",
    "years": 0,        # 경력 하한
    "limit": 20,
    "query": "데이터 엔지니어",
}

resp = httpx.get("https://www.wanted.co.kr/api/v4/jobs",
                 params=PARAMS, headers=HEADERS, timeout=15)
resp.raise_for_status()
data = resp.json()
out = Path("tests/fixtures/wanted_list.json")
out.parent.mkdir(parents=True, exist_ok=True)
out.write_text(json.dumps(data, ensure_ascii=False, indent=1), encoding="utf-8")
print(f"저장: {out}, 공고 수: {len(data.get('data', []))}")
print(json.dumps(data["data"][0], ensure_ascii=False, indent=1)[:2000])
```

Run: `uv run python scripts/probe_wanted.py`
Expected: fixture 저장 + 첫 공고의 실제 필드 구조 출력. **여기서 확인한 실제 필드명이 아래 Step 3 코드와 다르면 Step 3 코드를 실제 구조에 맞게 수정한다.** (403/차단 시: 헤더에 `"Referer": "https://www.wanted.co.kr/"` 추가 시도, 그래도 실패하면 사용자에게 보고하고 중단)

- [ ] **Step 2: 실패하는 테스트 작성** — `tests/test_wanted.py`

```python
import json
from pathlib import Path

from src.collectors.wanted import parse_list

FIXTURE = json.loads(
    Path("tests/fixtures/wanted_list.json").read_text(encoding="utf-8")
)


def test_parse_list_returns_postings():
    postings = parse_list(FIXTURE)
    assert len(postings) > 0
    p = postings[0]
    assert p.site == "wanted"
    assert p.id.startswith("wanted:")
    assert p.title
    assert p.company
    assert p.url.startswith("https://www.wanted.co.kr/wd/")
```

- [ ] **Step 3: 실패 확인 후 구현** — `src/collectors/wanted.py`

Run: `uv run pytest tests/test_wanted.py -v` → Expected: FAIL (`No module named 'src.collectors.wanted'`)

```python
"""원티드 수집기 — 내부 JSON API(v4) 사용. 개인 용도 소량 수집."""
import time

import httpx

from src.models import JobPosting

BASE = "https://www.wanted.co.kr"
HEADERS = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)",
    "Referer": "https://www.wanted.co.kr/",
}


def parse_list(data: dict) -> list[JobPosting]:
    postings = []
    for item in data.get("data", []):
        job_id = item["id"]
        postings.append(JobPosting(
            id=f"wanted:{job_id}",
            site="wanted",
            title=item.get("position", ""),
            company=item.get("company", {}).get("name", ""),
            location=item.get("address", {}).get("location", ""),
            experience="",  # 목록 응답에는 경력 정보 없음 (검색 조건으로 필터됨)
            url=f"{BASE}/wd/{job_id}",
            description="",
            posted_at="",
        ))
    return postings


def fetch_detail(job_id: int, client: httpx.Client) -> str:
    """상세 본문(주요 업무·자격 요건)을 가져온다. 실패하면 빈 문자열."""
    try:
        resp = client.get(f"{BASE}/api/v4/jobs/{job_id}", headers=HEADERS, timeout=15)
        resp.raise_for_status()
        detail = resp.json().get("job", {}).get("detail", {})
        parts = [detail.get(k, "") for k in ("intro", "main_tasks", "requirements")]
        return "\n\n".join(p for p in parts if p)
    except httpx.HTTPError:
        return ""


def search(keyword: str, limit: int = 20) -> list[JobPosting]:
    params = {
        "country": "kr",
        "job_sort": "job.latest_order",
        "locations": "seoul.all",
        "years": 0,
        "limit": limit,
        "query": keyword,
    }
    with httpx.Client() as client:
        resp = client.get(f"{BASE}/api/v4/jobs", params=params,
                          headers=HEADERS, timeout=15)
        resp.raise_for_status()
        postings = parse_list(resp.json())
        for p in postings:
            time.sleep(0.5)  # rate limit
            p.description = fetch_detail(int(p.id.split(":")[1]), client)
    return postings
```

**주의:** Step 1에서 확인한 실제 응답 필드가 다르면 여기 필드 접근을 맞춰 수정한다. `years=0`이 "신입 포함"으로 동작하는지 probe 출력에서 확인하고, 경력 상한 파라미터(`years=-1` 또는 별도 파라미터)가 있으면 반영한다.

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_wanted.py -v`
Expected: 1 passed

- [ ] **Step 5: 실제 호출 스모크 테스트 (수동 1회)**

Run: `uv run python -c "from src.collectors.wanted import search; ps = search('데이터 엔지니어', limit=3); [print(p.id, p.title, p.company, len(p.description)) for p in ps]"`
Expected: 공고 3건과 상세 본문 길이(0이 아닌 값 위주) 출력

- [ ] **Step 6: 커밋**

```powershell
git add src/collectors/wanted.py scripts/probe_wanted.py tests/test_wanted.py tests/fixtures/wanted_list.json
git commit -m "feat: 원티드 수집기 추가 (내부 v4 API, fixture 기반 테스트)"
```

---

### Task 4: 에이전트 (채점·요약)

**Files:**
- Create: `src/agent.py`
- Test: `tests/test_agent.py`

**Interfaces:**
- Consumes: `JobPosting`, `SeenStore` (Task 2), 수집기 `search` 함수 (Task 3)
- Produces:
  - `ScoredJob` dataclass — `id: str`, `site: str`, `title: str`, `company: str`, `url: str`, `score: int`(1~10), `reason: str`, `summary: str`
  - `async run_agent(config: dict, profile_text: str, collectors: dict[str, Callable], store: SeenStore) -> tuple[list[ScoredJob], list[str], list[str]]` — (채점 결과, 이 실행에서 에이전트에게 전달된 신규 공고 id 목록, 수집 실패 메시지 목록)
  - `extract_json(text: str) -> list[dict]` — 에이전트 응답에서 JSON 배열 추출 (fence 제거)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_agent.py`

에이전트 자체(LLM 호출)는 단위 테스트하지 않는다. 테스트 대상은 (a) 툴 핸들러가 수집→seen 필터→JSON 직렬화를 올바로 하는지, (b) 응답 JSON 파싱.

```python
import json
from pathlib import Path

from src.agent import build_search_tool, extract_json
from src.models import JobPosting
from src.store import SeenStore


def fake_search(keyword: str, limit: int = 20) -> list[JobPosting]:
    return [JobPosting(id="wanted:1", site="wanted", title=f"{keyword} 주니어",
                       company="A사", location="서울", experience="신입",
                       url="https://x/1", description="파이프라인 구축", posted_at="")]


async def test_search_tool_filters_seen_and_records_ids(tmp_path: Path):
    store = SeenStore(tmp_path / "seen.json")
    fetched_ids: list[str] = []
    handler = build_search_tool({"wanted": fake_search}, store, fetched_ids, [])

    result = await handler({"site": "wanted", "keyword": "데이터 엔지니어"})
    postings = json.loads(result["content"][0]["text"])
    assert postings[0]["id"] == "wanted:1"
    assert fetched_ids == ["wanted:1"]

    store.mark(["wanted:1"])
    result = await handler({"site": "wanted", "keyword": "데이터 엔지니어"})
    assert json.loads(result["content"][0]["text"]) == []


async def test_search_tool_unknown_site_returns_error_text(tmp_path: Path):
    handler = build_search_tool({}, SeenStore(tmp_path / "s.json"), [], [])
    result = await handler({"site": "wanted", "keyword": "x"})
    assert "사용할 수 없는 사이트" in result["content"][0]["text"]


async def test_search_tool_collector_error_recorded_as_failure(tmp_path: Path):
    def broken_search(keyword: str, limit: int = 20):
        raise RuntimeError("connection refused")

    failures: list[str] = []
    handler = build_search_tool({"wanted": broken_search},
                                SeenStore(tmp_path / "s.json"), [], failures)
    result = await handler({"site": "wanted", "keyword": "x"})
    assert "수집 실패" in result["content"][0]["text"]
    assert failures and "wanted" in failures[0]


def test_extract_json_strips_code_fence():
    text = '채점 결과입니다.\n```json\n[{"id": "wanted:1", "score": 8}]\n```'
    assert extract_json(text) == [{"id": "wanted:1", "score": 8}]
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_agent.py -v`
Expected: FAIL — `No module named 'src.agent'`

- [ ] **Step 3: 구현** — `src/agent.py`

```python
"""Claude Agent SDK 기반 채점 에이전트.

수집기를 커스텀 툴로 노출하고, 에이전트가 키워드를 바꿔가며 검색한 뒤
profile.md 대비 적합도를 JSON으로 출력한다.
"""
import json
import re
from dataclasses import asdict, dataclass
from typing import Callable

from claude_agent_sdk import (
    ClaudeAgentOptions,
    create_sdk_mcp_server,
    query,
    tool,
)

from src.models import JobPosting
from src.store import SeenStore


@dataclass
class ScoredJob:
    id: str
    site: str
    title: str
    company: str
    url: str
    score: int
    reason: str
    summary: str


def build_search_tool(collectors: dict[str, Callable], store: SeenStore,
                      fetched_ids: list[str], failures: list[str]):
    """search_jobs 툴 핸들러를 만든다. (테스트 가능하도록 분리)"""
    async def handler(args: dict) -> dict:
        site, keyword = args["site"], args["keyword"]
        if site not in collectors:
            return {"content": [{"type": "text",
                                 "text": f"사용할 수 없는 사이트: {site}. "
                                         f"가능한 값: {list(collectors)}"}]}
        try:
            postings = store.filter_new(collectors[site](keyword))
        except Exception as e:  # 사이트 단위로만 실패시키고 실행은 계속한다
            msg = f"{site} 수집 실패: {e}"
            if msg not in failures:
                failures.append(msg)
            return {"content": [{"type": "text",
                                 "text": f"{site} 수집 실패 — 이 사이트는 건너뛰어라."}]}
        for p in postings:
            if p.id not in fetched_ids:
                fetched_ids.append(p.id)
        payload = json.dumps([asdict(p) for p in postings], ensure_ascii=False)
        return {"content": [{"type": "text", "text": payload}]}
    return handler


def extract_json(text: str) -> list[dict]:
    """응답 텍스트에서 JSON 배열을 추출한다. 코드 fence 유무 모두 처리."""
    fence = re.search(r"```(?:json)?\s*(\[.*?\])\s*```", text, re.DOTALL)
    raw = fence.group(1) if fence else text[text.find("["):text.rfind("]") + 1]
    return json.loads(raw)


SYSTEM_PROMPT = """당신은 채용 공고 매칭 전문가다. search_jobs 툴로 공고를 검색해
지원자 프로필과 비교·채점한다.

규칙:
- 설정에 주어진 키워드로 각 사이트를 검색하고, 결과가 적으면 유사 키워드
  (예: "ETL", "데이터 플랫폼")로 1~2회 추가 검색한다.
- 툴이 반환한 공고만 다룬다. 공고를 지어내지 않는다. url·id는 툴 결과 값을 그대로 쓴다.
- 각 공고에 대해 프로필 대비 적합도 1~10점, 한 줄 이유, 3줄 이내 요약을 만든다.
- 신규 공고가 하나도 없으면 빈 배열 []을 출력한다.

최종 응답은 반드시 아래 형식의 JSON 배열 하나만 출력한다:
```json
[{"id": "...", "site": "...", "title": "...", "company": "...", "url": "...",
  "score": 8, "reason": "...", "summary": "..."}]
```"""


async def run_agent(
    config: dict, profile_text: str, collectors: dict[str, Callable],
    store: SeenStore,
) -> tuple[list[ScoredJob], list[str], list[str]]:
    fetched_ids: list[str] = []
    failures: list[str] = []
    handler = build_search_tool(collectors, store, fetched_ids, failures)
    search_jobs = tool(
        "search_jobs",
        "채용 사이트에서 키워드로 공고를 검색한다. 이미 알림 보낸 공고는 자동 제외된다.",
        {"site": str, "keyword": str},
    )(handler)
    server = create_sdk_mcp_server(name="jobs", version="1.0.0", tools=[search_jobs])

    mission = (
        f"다음 조건으로 신규 채용 공고를 찾아 채점하라.\n"
        f"- 검색 키워드: {config['job_keywords']}\n"
        f"- 사용 가능 사이트: {list(collectors)}\n"
        f"- 대상 경력: {config['experience']}, 지역: {config['locations']}\n\n"
        f"# 지원자 프로필\n{profile_text}"
    )
    options = ClaudeAgentOptions(
        system_prompt=SYSTEM_PROMPT,
        mcp_servers={"jobs": server},
        allowed_tools=["mcp__jobs__search_jobs"],
        max_turns=15,
    )

    result_text = ""
    async for message in query(prompt=mission, options=options):
        if type(message).__name__ == "ResultMessage":
            result_text = message.result or ""

    scored = [ScoredJob(**{k: item[k] for k in
                           ("id", "site", "title", "company", "url",
                            "score", "reason", "summary")})
              for item in extract_json(result_text)] if result_text.strip() else []
    return scored, fetched_ids, failures
```

**주의:** `claude_agent_sdk`의 `tool`/`create_sdk_mcp_server`/`ClaudeAgentOptions` 시그니처는 설치된 버전에서 `uv run python -c "import claude_agent_sdk; help(claude_agent_sdk.tool)"`로 확인하고, 다르면 실제 시그니처에 맞춘다 (예: 입력 스키마가 JSON Schema dict를 요구하면 `{"type": "object", "properties": {"site": {"type": "string"}, "keyword": {"type": "string"}}, "required": ["site", "keyword"]}` 형태로).

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_agent.py -v`
Expected: 4 passed

- [ ] **Step 5: 커밋**

```powershell
git add src/agent.py tests/test_agent.py
git commit -m "feat: Agent SDK 기반 채점 에이전트 추가 (search_jobs 커스텀 툴)"
```

---

### Task 5: 이메일 렌더링 + 발송

**Files:**
- Create: `src/mailer.py`
- Test: `tests/test_mailer.py`

**Interfaces:**
- Consumes: `ScoredJob` (Task 4)
- Produces:
  - `render_html(scored: list[ScoredJob], failures: list[str]) -> str` — 점수 내림차순 HTML
  - `send_email(subject: str, html: str, cfg: dict) -> None` — cfg는 config.yaml의 `email` 섹션 + 환경변수 `SMTP_USER`/`SMTP_PASSWORD` 사용. 실패 시 예외를 그대로 전파 (호출자가 로컬 백업 처리)

- [ ] **Step 1: 실패하는 테스트 작성** — `tests/test_mailer.py`

```python
from src.agent import ScoredJob
from src.mailer import render_html


def make_job(score: int, title: str) -> ScoredJob:
    return ScoredJob(id=f"wanted:{score}", site="wanted", title=title,
                     company="회사", url=f"https://x/{score}", score=score,
                     reason="스택 일치", summary="파이프라인 구축 포지션")


def test_render_html_sorted_by_score_desc_with_links():
    html = render_html([make_job(6, "낮은거"), make_job(9, "높은거")], [])
    assert html.index("높은거") < html.index("낮은거")
    assert 'href="https://x/9"' in html


def test_render_html_shows_failures():
    html = render_html([make_job(7, "공고")], ["saramin 수집 실패"])
    assert "saramin 수집 실패" in html
```

- [ ] **Step 2: 실패 확인**

Run: `uv run pytest tests/test_mailer.py -v`
Expected: FAIL — `No module named 'src.mailer'`

- [ ] **Step 3: 구현** — `src/mailer.py`

```python
"""HTML 이메일 렌더링 및 네이버 SMTP 발송."""
import html
import os
import smtplib
from email.mime.text import MIMEText

from src.agent import ScoredJob

SITE_LABEL = {"wanted": "원티드", "saramin": "사람인", "rocketpunch": "로켓펀치"}


def render_html(scored: list[ScoredJob], failures: list[str]) -> str:
    rows = []
    for job in sorted(scored, key=lambda j: j.score, reverse=True):
        rows.append(f"""
        <div style="border:1px solid #ddd;border-radius:8px;padding:12px;margin:10px 0">
          <div style="font-size:13px;color:#888">{SITE_LABEL.get(job.site, job.site)}
            &nbsp;|&nbsp; 적합도 <b>{job.score}/10</b></div>
          <div style="font-size:16px;margin:4px 0">
            <a href="{html.escape(job.url)}"><b>{html.escape(job.title)}</b></a>
            — {html.escape(job.company)}</div>
          <div style="font-size:13px;color:#444">이유: {html.escape(job.reason)}</div>
          <div style="font-size:13px;color:#444;white-space:pre-line">{html.escape(job.summary)}</div>
        </div>""")
    failure_html = ""
    if failures:
        items = "".join(f"<li>{html.escape(f)}</li>" for f in failures)
        failure_html = f'<hr><div style="color:#a00;font-size:12px"><ul>{items}</ul></div>'
    return f"<html><body>{''.join(rows)}{failure_html}</body></html>"


def send_email(subject: str, body_html: str, cfg: dict) -> None:
    user = os.environ["SMTP_USER"]
    password = os.environ["SMTP_PASSWORD"]
    msg = MIMEText(body_html, "html", "utf-8")
    msg["Subject"] = subject
    msg["From"] = cfg["from"]
    msg["To"] = cfg["to"]
    with smtplib.SMTP_SSL(cfg["smtp_host"], cfg["smtp_port"], timeout=30) as smtp:
        smtp.login(user, password)
        smtp.send_message(msg)
```

- [ ] **Step 4: 통과 확인**

Run: `uv run pytest tests/test_mailer.py -v`
Expected: 2 passed

- [ ] **Step 5: 커밋**

```powershell
git add src/mailer.py tests/test_mailer.py
git commit -m "feat: HTML 이메일 렌더링·네이버 SMTP 발송 추가"
```

**참고 (사용자 사전 작업):** 네이버 메일 → 환경설정 → POP3/IMAP 설정 → "POP3/SMTP 사용" 활성화. 2단계 인증을 쓰면 [네이버 내정보 → 보안설정 → 애플리케이션 비밀번호]에서 발급한 비밀번호를 `SMTP_PASSWORD`에 넣는다.

---

### Task 6: main 통합 + dry-run

**Files:**
- Create: `src/main.py`
- Test: 수동 통합 테스트 (dry-run → 실발송 1회)

**Interfaces:**
- Consumes: 모든 이전 태스크의 공개 인터페이스
- Produces: `uv run python -m src.main [--dry-run]` 진입점

- [ ] **Step 1: 구현** — `src/main.py`

```python
"""진입점: 에이전트 실행(툴로 수집 주도) → 결과 이메일 발송."""
import argparse
import asyncio
import datetime
import logging
from pathlib import Path

import yaml
from dotenv import load_dotenv

from src.agent import run_agent
from src.collectors import wanted
from src.mailer import render_html, send_email
from src.store import SeenStore

ROOT = Path(__file__).resolve().parent.parent
COLLECTOR_FUNCS = {"wanted": wanted.search}
# Task 8/9 완료 시 여기에 saramin/rocketpunch 추가


def setup_logging() -> logging.Logger:
    log_dir = ROOT / "logs"
    log_dir.mkdir(exist_ok=True)
    logging.basicConfig(
        filename=log_dir / "job_scout.log", level=logging.INFO,
        format="%(asctime)s %(levelname)s %(message)s", encoding="utf-8",
    )
    logging.getLogger().addHandler(logging.StreamHandler())
    return logging.getLogger("job_scout")


def main() -> None:
    parser = argparse.ArgumentParser()
    parser.add_argument("--dry-run", action="store_true",
                        help="이메일을 보내지 않고 콘솔에 출력, seen 기록 안 함")
    args = parser.parse_args()

    load_dotenv(ROOT / ".env")
    log = setup_logging()
    config = yaml.safe_load((ROOT / "config.yaml").read_text(encoding="utf-8"))
    profile = (ROOT / "profile.md").read_text(encoding="utf-8")
    store = SeenStore(ROOT / "data" / "seen.json")

    collectors = {site: COLLECTOR_FUNCS[site]
                  for site, enabled in config["sites"].items()
                  if enabled and site in COLLECTOR_FUNCS}

    log.info("에이전트 실행 시작 (사이트: %s)", list(collectors))
    scored, fetched_ids, failures = asyncio.run(
        run_agent(config, profile, collectors, store))
    for f in failures:
        log.warning(f)
    picked = sorted([s for s in scored if s.score >= config["min_score"]],
                    key=lambda s: s.score, reverse=True)[: config["top_n"]]
    log.info("신규 %d건, 채점 %d건, 발송 대상 %d건",
             len(fetched_ids), len(scored), len(picked))

    if args.dry_run:
        for job in picked:
            print(f"[{job.score}/10] {job.title} — {job.company}\n"
                  f"  {job.url}\n  이유: {job.reason}\n  {job.summary}\n")
        log.info("dry-run 종료 (발송·기록 없음)")
        return

    if not picked:
        store.mark(fetched_ids)  # 저점 공고도 재알림 방지
        log.info("발송할 공고 없음")
        return

    today = datetime.date.today().isoformat()
    subject = f"[job-scout] {today} 신규 공고 {len(picked)}건"
    body = render_html(picked, failures)
    try:
        send_email(subject, body, config["email"])
        log.info("이메일 발송 완료: %s", config["email"]["to"])
    except Exception:
        backup = ROOT / "reports" / f"{today}.html"
        backup.parent.mkdir(exist_ok=True)
        backup.write_text(body, encoding="utf-8")
        log.exception("이메일 발송 실패 — 백업 저장: %s", backup)
        raise
    store.mark(fetched_ids)


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 전체 테스트 회귀 확인**

Run: `uv run pytest -v`
Expected: 지금까지의 테스트 전부 passed

- [ ] **Step 3: dry-run 통합 테스트 (실제 LLM 호출 발생)**

사전 조건: `.env` 준비 (`ANTHROPIC_API_KEY` 있으면 사용, 없으면 Claude Code 로그인 인증).

Run: `uv run python -m src.main --dry-run`
Expected: 원티드 신규 공고들이 `[점수/10] 제목 — 회사` 형식으로 출력. 점수·이유가 프로필과 정합적인지 눈으로 확인. 이상하면 `SYSTEM_PROMPT`를 조정하고 재실행.

- [ ] **Step 4: 실발송 1회 확인**

사전 조건: `.env`에 `SMTP_USER`/`SMTP_PASSWORD` 설정, 네이버 SMTP 활성화(Task 5 참고).

Run: `uv run python -m src.main`
Expected: rkdrudals991@naver.com 수신함에 HTML 메일 도착, `data/seen.json` 생성됨. 직후 재실행하면 "발송할 공고 없음" 로그 (중복 방지 동작 확인).

- [ ] **Step 5: 커밋 + 푸시**

```powershell
git add src/main.py
git commit -m "feat: main 진입점 추가 (dry-run, seen 기록, 발송 실패 백업)"
git push
```

---

### Task 7: 작업 스케줄러 등록 + 운영 문서

**Files:**
- Create: `scripts/register_task.ps1`
- Modify: `README.md`

**Interfaces:**
- Consumes: `uv run python -m src.main` (Task 6)

- [ ] **Step 1: 등록 스크립트 작성** — `scripts/register_task.ps1`

```powershell
# job-scout를 매일 09:00에 실행하는 Windows 예약 작업을 등록한다.
# 사용법: powershell -ExecutionPolicy Bypass -File scripts\register_task.ps1 [-Time "09:00"]
param([string]$Time = "09:00")

$repo = Split-Path -Parent $PSScriptRoot
$uv = (Get-Command uv).Source
$action = "`"$uv`" run --directory `"$repo`" python -m src.main"

schtasks /Create /F /TN "job-scout-daily" /SC DAILY /ST $Time /TR $action
Write-Host "등록 완료. 확인: schtasks /Query /TN job-scout-daily"
Write-Host "즉시 테스트: schtasks /Run /TN job-scout-daily (이후 logs\job_scout.log 확인)"
```

- [ ] **Step 2: 등록 및 동작 확인**

Run: `powershell -ExecutionPolicy Bypass -File scripts\register_task.ps1`
Expected: `성공: 예약 작업 "job-scout-daily"이(가) 성공적으로 만들어졌습니다.`

Run: `schtasks /Run /TN job-scout-daily` 후 30초 뒤 `Get-Content logs\job_scout.log -Tail 5`
Expected: 방금 실행된 로그 라인 존재 (신규 0건이어도 정상)

- [ ] **Step 3: README에 운영 문서 추가**

README.md 끝에 추가:

```markdown
## 매일 자동 실행

​```powershell
powershell -ExecutionPolicy Bypass -File scripts\register_task.ps1 -Time "09:00"
​```

- 실행 기록: `logs\job_scout.log`
- 발송 실패 백업: `reports\YYYY-MM-DD.html`
- 중복 방지 기록 초기화: `data\seen.json` 삭제
- 해제: `schtasks /Delete /TN job-scout-daily /F`

## 사이트 추가 상태

| 사이트 | 상태 | 비고 |
|---|---|---|
| 원티드 | ✅ | 내부 JSON API |
| 사람인 | ⏳ | 오픈 API 키 발급 후 `config.yaml`에서 활성화 |
| 로켓펀치 | ⏳ | HTML 크롤링 |
```

- [ ] **Step 4: 커밋 + 푸시**

```powershell
git add scripts/register_task.ps1 README.md
git commit -m "feat: 작업 스케줄러 등록 스크립트와 운영 문서 추가"
git push
```

---

### Task 8: 사람인 수집기 (선행 조건: API 키 발급)

**선행 조건 (사용자 작업):** https://oapi.saramin.co.kr 에서 오픈 API 키 발급 신청 → 승인된 키를 `.env`의 `SARAMIN_API_KEY`에 입력. **키가 없으면 이 태스크는 건너뛰고 나중에 실행한다.**

**Files:**
- Create: `src/collectors/saramin.py`, `scripts/probe_saramin.py`
- Modify: `src/main.py`(COLLECTOR_FUNCS), `config.yaml`(sites.saramin: true)
- Test: `tests/test_saramin.py`, `tests/fixtures/saramin_list.json`

**Interfaces:**
- Consumes: `JobPosting` (Task 2)
- Produces: `search(keyword: str, limit: int = 20) -> list[JobPosting]`, `parse_list(data: dict) -> list[JobPosting]`

- [ ] **Step 1: probe 스크립트 작성·실행** — `scripts/probe_saramin.py`

```python
"""사람인 오픈 API 응답 확인 및 fixture 저장."""
import json
import os
from pathlib import Path

import httpx
from dotenv import load_dotenv

load_dotenv()
resp = httpx.get("https://oapi.saramin.co.kr/job-search", params={
    "access-key": os.environ["SARAMIN_API_KEY"],
    "keywords": "데이터 엔지니어",
    "loc_cd": "101000,102000",   # 서울, 경기
    "count": 20,
}, headers={"Accept": "application/json"}, timeout=15)
resp.raise_for_status()
data = resp.json()
out = Path("tests/fixtures/saramin_list.json")
out.write_text(json.dumps(data, ensure_ascii=False, indent=1), encoding="utf-8")
print(f"공고 수: {data['jobs']['count']}")
print(json.dumps(data["jobs"]["job"][0], ensure_ascii=False, indent=1)[:2000])
```

Run: `uv run python scripts/probe_saramin.py`
Expected: fixture 저장 + 첫 공고 구조 출력. 실제 필드명·지역 코드가 다르면 아래 구현을 맞춘다. (경력 필터 파라미터 `exp_cd`가 있으면 신입·주니어 코드 확인해 추가)

- [ ] **Step 2: 실패하는 테스트 작성** — `tests/test_saramin.py`

```python
import json
from pathlib import Path

from src.collectors.saramin import parse_list

FIXTURE = json.loads(
    Path("tests/fixtures/saramin_list.json").read_text(encoding="utf-8")
)


def test_parse_list_returns_postings():
    postings = parse_list(FIXTURE)
    assert len(postings) > 0
    p = postings[0]
    assert p.site == "saramin"
    assert p.id.startswith("saramin:")
    assert p.title and p.company and p.url.startswith("http")
```

- [ ] **Step 3: 실패 확인 후 구현** — `src/collectors/saramin.py`

Run: `uv run pytest tests/test_saramin.py -v` → FAIL 확인 후:

```python
"""사람인 수집기 — 공식 오픈 API 사용."""
import os

import httpx

from src.models import JobPosting


def parse_list(data: dict) -> list[JobPosting]:
    postings = []
    for job in data.get("jobs", {}).get("job", []):
        pos = job.get("position", {})
        postings.append(JobPosting(
            id=f"saramin:{job['id']}",
            site="saramin",
            title=pos.get("title", ""),
            company=job.get("company", {}).get("detail", {}).get("name", ""),
            location=pos.get("location", {}).get("name", ""),
            experience=pos.get("experience-level", {}).get("name", ""),
            url=job.get("url", ""),
            description=pos.get("job-code", {}).get("name", ""),
            posted_at=job.get("posting-date", ""),
        ))
    return postings


def search(keyword: str, limit: int = 20) -> list[JobPosting]:
    resp = httpx.get("https://oapi.saramin.co.kr/job-search", params={
        "access-key": os.environ["SARAMIN_API_KEY"],
        "keywords": keyword,
        "loc_cd": "101000,102000",
        "count": limit,
    }, headers={"Accept": "application/json"}, timeout=15)
    resp.raise_for_status()
    return parse_list(resp.json())
```

- [ ] **Step 4: 통과 확인 후 main에 연결**

Run: `uv run pytest tests/test_saramin.py -v` → passed

`src/main.py` 수정 — import에 `from src.collectors import saramin, wanted`, 그리고:

```python
COLLECTOR_FUNCS = {"wanted": wanted.search, "saramin": saramin.search}
```

`config.yaml`: `saramin: true`

- [ ] **Step 5: dry-run 확인 + 커밋 + 푸시**

Run: `uv run python -m src.main --dry-run` → 사람인 공고 포함 확인

```powershell
git add -A
git commit -m "feat: 사람인 수집기 추가 (공식 오픈 API)"
git push
```

---

### Task 9: 로켓펀치 수집기

**Files:**
- Create: `src/collectors/rocketpunch.py`, `scripts/probe_rocketpunch.py`
- Modify: `src/main.py`(COLLECTOR_FUNCS), `config.yaml`(sites.rocketpunch: true), `README.md`(상태 표)
- Test: `tests/test_rocketpunch.py`, `tests/fixtures/rocketpunch_list.html`

**Interfaces:**
- Consumes: `JobPosting` (Task 2)
- Produces: `search(keyword: str, limit: int = 20) -> list[JobPosting]`, `parse_html(html_text: str) -> list[JobPosting]`

로켓펀치는 API가 없어 검색 결과 HTML을 파싱한다. **셋 중 가장 깨지기 쉽다** — probe로 실제 HTML을 받아 fixture로 저장한 뒤, 실제 마크업의 CSS 선택자를 확인해 구현한다. Cloudflare 등으로 차단되면 사용자에게 보고하고 이 태스크를 보류한다 (수집 실패는 main에서 사이트 단위로 건너뛰므로 나머지 기능에 영향 없음).

- [ ] **Step 1: probe 스크립트 작성·실행** — `scripts/probe_rocketpunch.py`

```python
"""로켓펀치 채용 검색 HTML 확인 및 fixture 저장."""
from pathlib import Path

import httpx

resp = httpx.get("https://www.rocketpunch.com/jobs", params={
    "keywords": "데이터 엔지니어",
    "hiring_types": 0,   # 정규직 (probe 결과에 따라 조정)
}, headers={"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"},
    timeout=15, follow_redirects=True)
resp.raise_for_status()
Path("tests/fixtures/rocketpunch_list.html").write_text(resp.text, encoding="utf-8")
print(f"상태: {resp.status_code}, 길이: {len(resp.text)}")
print("공고 카드로 보이는 요소를 브라우저 개발자도구/파일에서 확인할 것")
```

Run: `uv run python scripts/probe_rocketpunch.py`
Expected: HTML fixture 저장. 저장된 파일을 열어 공고 카드의 실제 CSS 선택자(회사명·공고 제목·링크)를 확인하고 Step 3의 선택자를 실제 값으로 바꾼다. JS 렌더링이라 공고가 HTML에 없으면: 네트워크 탭에서 내부 API(`/api/...`)를 찾아 그것을 쓰고, 그것도 안 되면 사용자에게 보고 후 보류.

- [ ] **Step 2: 실패하는 테스트 작성** — `tests/test_rocketpunch.py`

```python
from pathlib import Path

from src.collectors.rocketpunch import parse_html

FIXTURE = Path("tests/fixtures/rocketpunch_list.html").read_text(encoding="utf-8")


def test_parse_html_returns_postings():
    postings = parse_html(FIXTURE)
    assert len(postings) > 0
    p = postings[0]
    assert p.site == "rocketpunch"
    assert p.id.startswith("rocketpunch:")
    assert p.title and p.company
    assert p.url.startswith("https://www.rocketpunch.com")
```

- [ ] **Step 3: 실패 확인 후 구현** — `src/collectors/rocketpunch.py`

Run: `uv run pytest tests/test_rocketpunch.py -v` → FAIL 확인 후 (선택자는 Step 1에서 확인한 실제 값으로 교체):

```python
"""로켓펀치 수집기 — 검색 결과 HTML 파싱. 마크업 변경에 취약."""
import httpx
from bs4 import BeautifulSoup

from src.models import JobPosting

BASE = "https://www.rocketpunch.com"
HEADERS = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"}


def parse_html(html_text: str) -> list[JobPosting]:
    soup = BeautifulSoup(html_text, "html.parser")
    postings = []
    # 아래 선택자는 probe fixture에서 확인한 실제 마크업으로 교체할 것
    for card in soup.select(".company.item"):
        link = card.select_one("a.job-title")
        company = card.select_one("a.company-name strong, .company-name")
        if not link or not company:
            continue
        href = link.get("href", "")
        job_id = href.rstrip("/").split("/")[-2] if "/jobs/" in href else href
        postings.append(JobPosting(
            id=f"rocketpunch:{job_id}",
            site="rocketpunch",
            title=link.get_text(strip=True),
            company=company.get_text(strip=True),
            location="",
            experience="",
            url=f"{BASE}{href}" if href.startswith("/") else href,
            description=card.get_text(" ", strip=True)[:500],
            posted_at="",
        ))
    return postings


def search(keyword: str, limit: int = 20) -> list[JobPosting]:
    resp = httpx.get(f"{BASE}/jobs", params={"keywords": keyword},
                     headers=HEADERS, timeout=15, follow_redirects=True)
    resp.raise_for_status()
    return parse_html(resp.text)[:limit]
```

- [ ] **Step 4: 통과 확인 후 main에 연결**

Run: `uv run pytest tests/test_rocketpunch.py -v` → passed

`src/main.py`의 `COLLECTOR_FUNCS`에 `"rocketpunch": rocketpunch.search` 추가, `config.yaml`의 `rocketpunch: true`, README 상태 표의 로켓펀치를 ✅로.

- [ ] **Step 5: 전체 회귀 + dry-run + 커밋 + 푸시**

Run: `uv run pytest -v` → 전부 passed
Run: `uv run python -m src.main --dry-run` → 3사이트 공고 확인

```powershell
git add -A
git commit -m "feat: 로켓펀치 수집기 추가 (HTML 파싱)"
git push
```
